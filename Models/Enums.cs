namespace WpfApp1.Models
{
    public enum Role { SuperUser, Manager, Employee }
    public enum Priority { Low, Medium, High, Critical }
    public enum TaskStatus { New, InProgress, Blocked, Completed }
}
