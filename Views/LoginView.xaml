<UserControl x:Class="WpfApp1.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Task Management System" FontSize="28" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,32"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="24">
            <StackPanel>
                <TextBlock Text="Login" FontSize="20" FontWeight="SemiBold" 
                           HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <StackPanel Orientation="Vertical" Margin="0,0,0,12">
                    <TextBlock Text="Username" Margin="0,0,0,4" FontWeight="Medium"/>
                    <TextBox x:Name="UsernameTextBox" Height="32" Padding="8"/>
                </StackPanel>
                
                <StackPanel Orientation="Vertical" Margin="0,0,0,16">
                    <TextBlock Text="Password" Margin="0,0,0,4" FontWeight="Medium"/>
                    <PasswordBox x:Name="PasswordBox" Height="32" Padding="8"/>
                </StackPanel>

                <Button x:Name="LoginButton" Content="Sign In" Width="120" Height="36" FontSize="14" Margin="0,0,0,8"/>

                <TextBlock x:Name="ErrorMessageText" Foreground="Red" 
                           HorizontalAlignment="Center" FontSize="12" 
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <TextBlock Text="Demo Users:" FontWeight="Medium" Margin="0,0,8,0"/>
            <TextBlock Text="super/super123 (Super User)" Margin="0,0,16,0"/>
            <TextBlock Text="manager/manager123 (Manager)" Margin="0,0,16,0"/>
            <TextBlock Text="alice/alice123 (Employee)"/>
        </StackPanel>
    </Grid>
</UserControl>


